local included = pcall(debug.getlocal, 4, 1)
local T = require 'test'
--#
--# == *??.??*(_String_, _String_)
--# ??
--#
--# === Arguments
--# [width="72%"]
--# |===
--# |??| ??
--# |??| ??
--# |===
--#
--# === Returns
--# [width="72%"]
--# |===
--# |??| ??
--# |===
local ??_?? = function()
  T.is_function(??.??)
  local x = "one"
  local y = "two"
  local z = ??.??(x, y)
  T.equal(z, "one\ntwo")
end
if included then
  return function()
    T['??.??'] = ??_??
  end
else
  T['??.??'] = ??_??
end
